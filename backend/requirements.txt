# Direct dependencies, pinned to the versions actually tested on Python 3.12.7 (macOS).
# Full transitive lock: requirements.lock.txt
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3        # enables the diagnostic `uvicorn --env-file` alternative
python-multipart==0.0.32
openpyxl==3.1.5
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
aiosqlite==0.22.1
groq==1.7.0
httpx==0.28.1
email-validator==2.3.0     # M2 offline email syntax validation (check_deliverability=False)

# --- test-only ---
pytest==9.1.1
pytest-asyncio==1.4.0
