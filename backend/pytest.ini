[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    live: tests that call the real Groq API (skipped unless GROQ_API_KEY and RUN_LIVE_GROQ=1)
